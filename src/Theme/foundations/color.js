import { theme } from '@chakra-ui/react';

const heading = theme.colors.gray['600'];
const primary = theme.colors.green['500'];
const text = theme.colors.gray['500'];
const textHighlight = theme.colors.gray['700'];

const Colors = {
  colors: {
    brand: theme.colors.green,
    heading,
    primary,
    text,
    textHighlight,
  },
};

export default Colors;
